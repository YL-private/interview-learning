## 为什么 data 需要是函数？
组件复用时所有组件实例都会共享 data，如果 data 是对象的话，就会造成一个组件修改 data 以后会影响到其他所有组件，所以需要将 data 写成函数，每次用到就调用一次函数获得新的数据。
当我们使用 new Vue() 的方式的时候，无论我们将 data 设置为对象还是函数都是可以的，因为 new Vue() 的方式是生成一个根组件，该组件不会复用，也就不存在共享 data 的情况了。

## 状态管理模式
1. Vuex是一个专为‌Vue.js应用程序开发的状态管理模式，用于管理组件之间的通信和数据共享。‌ Vuex采用集中式存储管理应用的所有组件状态，并以相应的规则保证状态以一种可预测的方式发生变化。它解决了组件之间同一状态的共享问题，使得组件可以与‌store通讯。

## wathc和computed区别
1. computed 是计算属性，依赖其他属性计算值，并且 computed 的值有缓存，只有当计算值变化才会返回内容。

2. watch 监听到值的变化就会执行回调，在回调中可以进行一些逻辑操作。

## history和hash区别
1. hash模式url带#，改变#后面的参数页面不会刷新，因为#后面的参数不会被搜索引擎抓取，SEO不友好
2. history模式改变url会刷新页面，更加美观，SEO更好