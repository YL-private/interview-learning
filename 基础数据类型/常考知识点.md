## 七大基础数据类型
String Number Boolean BigInt undefined null Symbol

## 引用数据类型
Function Array Object

## 深拷贝和浅拷贝
1. 浅拷贝只复制对象的第一层属性。如果属性是基本类型（如字符串、数字、布尔值），则直接复制值；如果属性是引用类型（如对象、数组、函数等），则复制引用（内存地址），而不是复制实际的对象。
2. 深拷贝会递归复制对象的所有层级属性，包括嵌套对象或引用类型的数据。复制后的对象和原对象完全独立，修改其中一个不会影响另一个。